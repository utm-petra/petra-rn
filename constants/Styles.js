import { StyleSheet } from "react-native";

const styleObj = {
  container: {
    flex: 1,
    backgroundColor: "#fff"
  },
  list: {
    flex: 1
  },
  map: { flex: 1 }
};

export default StyleSheet.create(styleObj);
